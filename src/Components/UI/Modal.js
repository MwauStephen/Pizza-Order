import React from "react";

const BackDrop = () => {
  return <div>{props.children}</div>;
};

const ModalOverlay = () => {
  return <div>{props.children}</div>;
};

const Modal = () => {
  return;
};

export default Modal;
